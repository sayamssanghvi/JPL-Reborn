<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zodiac.sanghvi.jplreborn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchActivity" />
        <activity android:name=".SplashActivty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_Option" />
        <activity android:name=".PlayerActivity" />
        <activity android:name=".FeaturesActivity" />
        <activity android:name=".Admin_Vistor" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Admin_MatchList" />
        <activity android:name=".Audience_First" />
        <activity android:name=".Admin_BatBall" />
        <activity android:name=".Admin_ScoreCard"></activity>
    </application>

</manifest>